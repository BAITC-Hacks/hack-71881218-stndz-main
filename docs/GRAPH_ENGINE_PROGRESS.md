# Graph Intelligence Engine — состояние разработки

**Обновлено:** 2026-09-23 · **Владелец:** Ali (Участник 1)

Это рабочий журнал. При старте новой сессии читать первым, затем `analytics/CLAUDE.md`, затем `docs/GRAPH_ENGINE_PLAN.md`.

---

## Текущая фаза

**PHASE 00 — Technical Audit & Architecture: ЗАВЕРШЕНА.**
**PHASE 01 — Graph Foundation: реализация и smoke-проверки выполнены; D-005 остаётся открытым для следующего checkpoint.**
**PHASE 02 — Graph Feature Engineering: реализация и smoke-проверки выполнены.**
**PHASE 03 — Role Detection & Explainability: реализована и проверена; D-003 принят (вариант г).**
**Следующая: PHASE 04 — Community Detection; перед запуском требуется D-005 от Участника 3.**

**Границы зоны (уточнено Ali 2026-09-23).** `pipeline.py` в корне и `frontend/` загружены участником, отвечающим за фронтенд, и к Graph Intelligence отношения не имеют. Они **не** являются базой для моей работы, не ревьюятся и не редактируются. Мой движок разрабатывается с нуля в `analytics/` по плану `docs/GRAPH_ENGINE_PLAN.md`.

## Инфраструктура сессии (2026-09-23)

- **Хук актуальности репозитория.** `.claude/hooks/git-remote-status.sh`, подключён в `.claude/settings.local.json` на события `SessionStart` и `UserPromptSubmit`. Делает `git fetch --prune` и подаёт в контекст: ветка, отставание/опережение, чистота дерева, а при отставании — список входящих коммитов и файлов. **Сознательно не делает `git pull`** — см. D-011.
- **Агенты workstream** в `.claude/agents/`: `graph-data-auditor` (замеры по данным без выгрузки датасета в контекст), `spec-compliance-checker` (проверка выгрузок против ТЗ и контракта), `aml-logic-reviewer` (ревью аналитической логики на доменные дефекты).
- `.claude/settings.local.json` добавлен в `.gitignore` (одна строка в общем файле — вынесено на согласование).

---

## Карта фаз

| Фаза | Содержание | Статус |
|---|---|---|
| PHASE 00 | Technical Audit & Architecture | **DONE** |
| PHASE 01 | Graph Foundation — loader, валидация, сборка графа, каркас пайплайна | **IMPLEMENTED · D-005 pending** |
| PHASE 02 | Graph Feature Engineering — структурные и денежные признаки | **DONE · smoke checks** |
| PHASE 03 | Role Detection & Explainability — правила ролей, `role_score`, `evidence` | **DONE · D-003 вариант г** |
| PHASE 04 | Community Detection — Louvain, гипотезы кластеров | не начата · ждёт D-005 |
| PHASE 05 | Priority & Temporal Intelligence — приоритет, `fast_share`, `sync_in_max` | не начата · блокирована D-008 |
| PHASE 06 | CSV Exports, Tests & Integration — 3 CSV, `graph.json`, pytest | не начата |

Сквозная тема **Blind Spot Intelligence** (учёт пределов наблюдаемости) не выделена в отдельную фазу намеренно: она реализуется внутри PHASE 01 (инвариант границы обхода), PHASE 02 (`ext_inflow`, флаги) и PHASE 03 (роль `boundary`, оговорки в `evidence`).

## PHASE 01 — Graph Foundation — 2026-09-23

- Добавлены `analytics/config.py`, `loader.py`, `graph.py`, `export.py` и `run_pipeline.py`.
- Пайплайн строит один `DiGraph` со всеми 2 248 узлами; проверки подтвердили 19 изолированных узлов, 16 компонент с рёбрами (крупнейшие: 1 877 и 270 узлов), 35 компонент всего.
- Сверка `edges` ↔ `transactions` сравнивает пары, суммы и число транзакций; ошибки данных приводят к `ValueError`.
- Инвариант границы обхода проверяется при сборке: все 444 узла `depth == 4` имеют `out_deg == 0`.
- На checkpoint PHASE 01 экспорты содержали заглушки; после PHASE 03 роли и evidence заполнены, а кластер и приоритет остаются заглушками. `graph.json` использует базовую схему командного плана; поля из `[ADD]` / `[CLARIFY]` не добавлялись до подтверждения Участником 2.
- `D-005` остаётся внешним блокером checkpoint: `requirements.txt` вне зоны движка, пиновку `networkx==3.7` должен согласовать и внести Участник 3.

## PHASE 02 — Graph Feature Engineering — 2026-09-23

- Добавлен `analytics/features.py`: взвешенный PageRank, точный направленный betweenness, `seed_payers`, `seed_reach2` и `retention`.
- Признаки считаются на том же `GraphContext` с 2 248 узлами; `nx.hits` не используется.
- `seed_reach2` проверен по аудированной частотной таблице: `{0: 1283, 1: 790, 2: 140, 3: 18, 4: 15, 7: 2}`; `seed_payers > 0` у 490 узлов.
- В feature-колонках нет NaN, кроме 42 значений `pass_through`; PageRank суммируется в 1, все остальные численные P0-признаки конечны.
- Полный прогон занял 1.54 s, менее лимита PHASE 02 в 5 s. CSV/JSON обновляются из расширенной таблицы признаков.
- `retention` при отсутствующем входящем потоке получает технический ноль; `pass_through` сохраняет NaN. Правило `terminal` использует retention только при `in_deg > 0`. В `graph.json` retention не добавлялся до подтверждения схемы Участником 2.

## PHASE 03 — Role Detection & Explainability — 2026-09-23

- D-003 принят: `terminal` требует `depth < 4`, `in_deg > 0`, `retention ≥ 0.8`; денежная клауза `consolidator` требует `in_deg ≥ 3` и `in_kzt ≥ 613 779 KZT`.
- Добавлены `analytics/roles.py` и `analytics/evidence.py`; `run_pipeline.py` теперь записывает рабочие роли и объяснения, сохраняя заглушки только для будущих cluster/priority полей.
- Распределение на 2 248 узлах: `coordinator` 15, `distributor` 49, `consolidator` 69, `transit` 67, `terminal` 1 158, `boundary` 444, `peripheral` 446.
- Доля входящих денег в `peripheral` — 16.33 %; все 444 boundary отделены от terminal, seed в transit нет.
- Все 2 248 evidence непустые, содержат числа и не превышают 161 символа; у 81 seed есть оговорка о невидимых входящих, у 444 boundary — о необследованных исходящих.
- `.venv/bin/python -m pytest tests/analytics -q`: 5 passed. Полный прогон завершился успешно; повторный выход записан во временный каталог, рабочая `output/` не перезаписана.

---

## PHASE 00 — что сделано

### Выполненные задачи

- [x] Проверено фактическое содержимое репозитория (архивов `starter.zip` / `data.zip` нет — материалы распакованы).
- [x] Изучено ТЗ (`context/TASK.md`), стартовый код, `HACKALEM_TEAM_PLAN.md`.
- [x] Поднято изолированное окружение `.venv` (уже в `.gitignore`), зафиксированы версии библиотек.
- [x] Проверены все числовые утверждения ТЗ по реальным parquet — 11 из 11 подтверждены.
- [x] Разобрана семантика недокументированного поля `edges.depth`.
- [x] Правила ролей из team plan §4.3 прогнаны на данных — распределение воспроизведено точно.
- [x] Замерена производительность всех тяжёлых алгоритмов.
- [x] Проверен детерминизм Louvain и betweenness.
- [x] Спроектирована архитектура модуля и контракт выдачи.
- [x] Созданы `analytics/CLAUDE.md`, `docs/GRAPH_ENGINE_PLAN.md`, `docs/GRAPH_DATA_CONTRACT.md`, `docs/GRAPH_DECISIONS.md`, этот файл.

### Результаты проверок данных

Данные чистые: NULL нет, дублей пар в `edges` нет, петель нет, все `src`/`dst` присутствуют в `nodes`, агрегация `transactions` → `edges` сходится до копейки по суммам и по `n_tx`. Транзакций ниже порога 5 000 KZT нет.

Подтверждено против ТЗ: 2 248 узлов / 3 119 рёбер / 4 840 транзакций / 81 seed / 365 890 012.01 KZT / колена 81-472-462-789-444 / 19 изолированных seed / 12 receive-only seed / 72 узла с `pass_through ∈ [0.8, 1.2]` / 8 сообществ с >1 seed / максимум 116 получателей / максимум 24 плательщика.

Симуляция правил ролей team plan §4.3 — факт против ожидания плана: coordinator 15/~15, distributor 49/~49, consolidator 50/~50, transit 67/~67, terminal 1073/~1070, boundary 444/444, peripheral 550/~550.

### Незавершённое в PHASE 00

- [x] **D-003** (вариант г: удержание для `terminal` и денежная клауза `consolidator`) — выбран по поручению Ali 2026-09-23, реализован в PHASE 03.
- [ ] **D-008** (перебалансировка весов приоритета) — ожидает решения Ali.
- [ ] **D-005** (пиновка `networkx==3.7` в `requirements.txt`) — ожидает согласования с Участником 3, файл вне моей зоны.
- [ ] Подтверждение контракта `graph.json` (`docs/GRAPH_DATA_CONTRACT.md`) Участником 2 — дополнения помечены `[ADD]` / `[CLARIFY]`.

---

## Обнаруженные проблемы

| ID | Проблема | Severity | Статус |
|---|---|---|---|
| F1 | Правила ролей не используют суммы: `peripheral` поглощает 31.3 % входящих денег графа (550 узлов, 114.7 млн KZT), 8 из топ-10 по PageRank — `peripheral` | высокая | D-003 принят; PHASE 03 снизила долю `peripheral` до 16.33 % |
| F2 | `nx.hits()` падает с `PowerIterationFailedConvergence` на этих данных, хотя рекомендован в `starter/README.md` | средняя | закрыта (D-004) |
| F3 | Louvain версионно-зависим: 91 сообщество на nx 3.7 против 87 в team plan; без `seed` — 87/89/86 | средняя | решение предложено (D-005), ждёт Участника 3 |
| F4 | `seed_reach2` равен нулю у 57 % узлов — не тянет вес 0.15 в приоритете | средняя | решение предложено (D-008) |
| F5 | `sync_in_max / 5` вырождается: медиана 1, p99 = 3, максимум 7 | низкая | решение предложено (D-008) |
| F6 | `fast_share` определён неоднозначно; наивная реализация даёт медиану 0.00 | низкая | закрыта спецификацией (D-009) |
| F7 | `gid` превышает 2^53 — в JSON только строкой | высокая для UI | закрыта (D-006) |
| F8 | 9 seed получают роль `terminal` при заведомо неполных входящих | низкая | PHASE 03 добавляет оговорку о невидимых входящих в evidence каждого seed |
| F9 | Производительность не является ограничением (≈1 s против бюджета 300 s) | — | закрыта (D-007) |
| F10 | **README датасета, на который ссылается ТЗ («Полное описание полей — в README.md датасета»), в репозитории отсутствует** | средняя | семантика полей восстановлена замерами; `edges.depth` разобран, см. план §1.3 |
| F11 | ~~Коллизия зон: `pipeline.py` в `main`~~ — **снято 2026-09-23**: файл загружен участником по фронтенду по ошибке, к моей зоне не относится | — | закрыта, вне зоны |
| F12 | `requirements.txt` в `main` содержит `networkx>=3.1` без точной пиновки — кластеризация невоспроизводима на машине жюри | средняя | D-005, файл в зоне Участника 3 |

### Расхождения ТЗ с фактическими данными

| ТЗ | Факт |
|---|---|
| «354 узла отдают больше, чем по данным получили» | 377 (из них 42 seed) |
| «16 слабосвязных компонент» | 16 среди узлов с рёбрами; 35 с учётом 19 изолированных |

Оба расхождения безопасны, но в README идут наши числа с формулой расчёта.

---

## Результаты тестирования

**Автотесты Graph Engine:** `.venv/bin/python -m pytest tests/analytics/test_roles.py -q` — 3 passed.

Для PHASE 01 запущен `.venv/bin/python run_pipeline.py` и выполнены smoke-проверки: схемы/строки CSV, число узлов и рёбер в JSON, строковые GID, 19 флагов `isolated`, 444 флага `truncated_by_depth`, конечность JSON-чисел, одинаковые SHA-256 CSV после повторного запуска. `pytest` не запускался.

Для PHASE 02 выполнены smoke-проверки на выходных данных: точное совпадение распределения `seed_reach2` и числа узлов с `seed_payers`, PageRank на полном графе, 42 NaN только в `pass_through`, остальные P0-признаки конечны; замер полного прогона — 1.54 s.

Для PHASE 03 полный `run_pipeline.py` проверил число и распределение ролей, диапазон `role_score`, долю входящих в `peripheral`, границу обхода, seed-исключение и все требования к evidence. Выход тестового прогона писался во временный каталог.

Проведённые проверки — диагностические скрипты аудита, выполненные вручную во временном каталоге сессии (в репозиторий не коммитились). Их содержательные результаты перенесены в `docs/GRAPH_ENGINE_PLAN.md` §1–2. Проверки, которые станут автотестами в PHASE 06, перечислены в `docs/GRAPH_DATA_CONTRACT.md` §8.

**Окружение замеров:** Python 3.14.4, pandas 3.0.6, numpy 2.5.3, networkx 3.7, pyarrow 25.0.1, scipy 1.18.1, macOS arm64.

> Замечание: версии в `.venv` — свежайшие на момент аудита и **не** совпадают с тем, что получит жюри по текущему `requirements.txt` (там `>=`). Это и есть предмет D-005.

---

## Следующие действия

1. Получить от Участника 3 пиновку `networkx==3.7` (**D-005**) до PHASE 04.
2. Передать Участнику 2 `docs/GRAPH_DATA_CONTRACT.md` на подтверждение до расширения JSON-контракта.
3. Выбрать рекомендованный вариант **D-008** перед PHASE 05; решение может принять Ali.

### Definition of Done для PHASE 01

- [x] `python run_pipeline.py` отрабатывает без ошибок и пишет 3 CSV со схемой ТЗ и `graph.json` (роли-заглушки)
- [x] `nodes_roles.csv` содержит ровно 2 248 строк
- [x] `G.number_of_nodes() == 2248`, изолированных ровно 19
- [x] сверка `edges` ↔ `transactions` проходит; расхождения обрабатываются через `ValueError`, не `assert`
- [x] компоненты: 16 среди узлов с рёбрами (1 877 / 270 / …), 35 с изолированными
- [x] инвариант «все 444 узла `depth == 4` имеют `out_deg == 0`» проверяется в рантайме
- [x] все числовые пороги находятся в `analytics/config.py`; в теле функций числовых констант нет
- [x] в `analytics/` нет ни одного литерального GID

### Definition of Done для PHASE 02

- [x] во feature-колонках нет NaN, кроме ожидаемых 42 значений `pass_through`
- [x] `nx.hits` не используется
- [x] все graph features рассчитаны на одном графе из 2 248 узлов
- [x] полный прогон занимает менее 5 s

### Definition of Done для PHASE 03

- [x] все 2 248 узлов имеют роль из словаря и `role_score ∈ [0.5, 1]`
- [x] 444 узла имеют роль `boundary`; ни один не `terminal`
- [x] seed не получают роль `transit`
- [x] доля входящих денег в `peripheral` — 16.33 %, ниже 20 %
- [x] evidence непустой, с числами, ≤ 200 символов; ограничения seed и boundary обозначены
- [x] unit-тесты PHASE 03 проходят, полный пайплайн smoke-check успешен
